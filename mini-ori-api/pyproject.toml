[project]
name = "mini-ori-api"
version = "0.1.0"
description = "FastAPI + SQLite implementatie voor mini-ori-koop OAS"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn==0.32.1",
  "sqlmodel==0.0.22",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
